# Placeholder for optional mesh files (.dae/.stl)

The current model uses primitive geometry (boxes/cylinders), so no meshes are
required yet. Drop visual meshes here and reference them from the xacro if you
later want a higher-fidelity look.
